# Secrets — never commit real keys
api_key
*.key
keys/
/config.toml
/registry.json
.env

# Build artifacts
target/
dist/
*.tar.gz


# OS / editor
.DS_Store
*.swp

# Build cache inside the vendored fork source
codex-src/**/target/
codex-src/codex-rs/target/
